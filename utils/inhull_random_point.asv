function r = inhull_random_point(p, n)
% p = convex hull point;
% n = return point number;
r = zeros(2,n);

for i = 1:n
   middle = mean(p,2);
   while(1)
   nomi = normrnd(middle, 1);
   kkk = convhulln(p);
   in = inhull(nomi, p, kkk);
    i
   end
end
